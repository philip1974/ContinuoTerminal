# get-buffer-snapshot

Tests for SessionManager.getBufferSnapshot(sessionId, options?) — pull-based raw byte buffer snapshot for non-MCP consumers (Continuo renderer attach via IPC). Complements readOutput (line-based MCP) — see ../read-output/.
